# 화이트리스트 방식: 이미지 실행에 필요한 것만 명시적으로 허용
# COPY . . 이후 컨테이너 안에 새 로컬 파일(백업, node_modules 등)이
# 실수로 다시 딸려 들어가는 걸 막기 위해 블록리스트 대신 이 방식을 사용
*
!app/
!app/**
!worker.py
!requirements.txt

# app/ 안에 딸려올 수 있는 캐시 파일은 다시 제외
**/__pycache__/
**/*.pyc
